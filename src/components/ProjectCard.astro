---
const { imgSrc, imgAlt, title, description } = Astro.props;
---

<article class="bg-[#242424] rounded-3xl w-[30%]">
    <div class="p-6">
        <img src={imgSrc} alt={imgAlt} />
        <p class="text-xl font-semibold mt-4">{title}</p>
        <p class="text-[#16px] mt-1 text-[#DADADA]">
            {description}
        </p>
    </div>
</article>
